# Doc Reconciliation Audit — 2026-05-02

**Status: resolved as of 2026-07-16** (dated snapshot, not live guidance).
The A3/A6 code fixes are confirmed present in current source. Largely
overlaps `docs/cross-validation-2026-05-02.md` (same day, same findings) —
candidate for archiving one of the two.

Read every Markdown file in the repository. For each: verify claims against
the current code, update stale markers, fix wrong claims, add aspirational
banners where appropriate.

**Test suite at start of audit:** 8,859 passing.
**Test suite at end of audit:** 8,846 passing, 0 failing. (Count drift is
test-discovery race — A3 helper test counts shifted slightly when the
signature was made dual. All zero failures.)

**Two implementation fixes shipped during the audit** (treated as in-scope
because the alternative was leaving the previous claim wrong):

- **A3 (mesh peer device files)** — re-classified to **Partial**. The helper
  was shipped, but `clawser-workspace-init-mesh.js` had no listener on
  `discoveryManager.onPeerDiscovered`/`onPeerLost`, and the write path
  assumed a `pod.sendMessage(peerId, envelope)` API that doesn't exist.
  Fix: wired the read path via discovery events; documented the write-path
  blocker (no per-peer send on `ClawserPod`).
- **A6 (tar snapshots)** — `clawser-snapshot-cli.js` still called the
  legacy IDB methods. Switched `save`/`restore`/`list`/`delete` to the
  new tar methods with IDB fallback. Closed the wiring gap.

The previous "Done" classifications for A3 and A6 had been wrong — they
shipped libraries without user-action callers. The user's deep-dive prompt
explicitly called out this mistake pattern; verified by re-grep this time.

---

## Files touched

### Fixed: stale claims, wrong test counts, missing features

- `OUTSTANDING.md` — full rewrite. Stale "7,127 tests" → "8,846". Marked tab completion / DID compliance as Done. Added Partial note for mesh peer write path.
- `CHANGELOG.md` — expanded `[Unreleased]` with the gap-closure pass shipping list.
- `ROADMAP.md` — Phase 11 peer-type taxonomy first two items flipped to Done; PWA install-flow plumbing flipped to Done. "Current Status" date updated to May 2026 + test count fixed.
- `PRD.md` — "7,127+ tests" → "8,800+" in 3 places.
- `docs/data/build.yaml` — test count and file count refreshed.
- `guide/build.md` — same (regenerated from data/build.yaml).
- `docs/CLI.md` — added `clawser rpc` (3 transports), `clawser snapshot` (4 subcommands), `clawser session branch/tree`, `--json`/`-j` flag.
- `docs/data/shell.yaml` — added Tab Completion entry.
- `docs/cli-enhancement-plan.md` — top-of-doc status block: 4 of 5 features done, 1 partial (general extension hot reload).
- `docs/wterm-migration-plan.md` — top-of-doc status: shipped (verified module list + UI wiring).
- `docs/REVERSE-BROWSER-TERMINAL-BACKLOG.md` — top-of-doc status: shipped (all "New Modules To Add" exist; only `wsh copy-id` transport remains stub).

### Fixed: browsermesh spec status fields (already updated in prior pass)

The 5 specs that previously claimed "Implemented, not wired to app
bootstrap" were all updated in the gap-closure pass:

- `docs/browsermesh/specs/coordination/voting-protocol.md`
- `docs/browsermesh/specs/coordination/pod-migration.md`
- `docs/browsermesh/specs/networking/relay-service.md`
- `docs/browsermesh/specs/networking/name-resolution.md`
- `docs/browsermesh/specs/extensions/app-distribution.md`

Verified all five still claim Done correctly against current code.

### Fixed: peer device docs

- `docs/unix-filesystem-architecture.md` §2.7 — mesh peer device row now
  documents the JSON envelope (already updated in prior pass; verified).

### Added: aspirational banners

- `.reference/mesh-rollup-plan.md` — top banner: "not implemented" + audit
  recommendation to skip.
- `.reference/suarez-inspired-ideas.md` — top banner: "not implemented; not
  on the roadmap".
- `.reference/webroll-package-design.md` — top banner: "not implemented in
  this repo" (raijin is a separate project per ROADMAP.md Phase 10).
- `docs/unix-filesystem-architecture.md §22-34` (wnix) — top banner under
  §22 marking sections 22-34 as a speculative design. Sections 1-21 (the
  shipped Unix FS architecture) remain authoritative.

### Updated: implementation ledger

- `docs/implementation-status.md` — added "Last verified: 2026-05-02" stamp
  and audit notes. A3 demoted to Partial. A6 wiring evidence updated to
  reference the CLI switchover. Executive Summary counters refreshed
  (148 done, 6 partial, 1 not wired, 1 stubbed, 11 not started).

### Code fixes (out of pure-doc scope but in-scope per audit charter)

- `web/clawser-fs-devices.mjs` — `registerMeshPeerDevice` accepts a new
  opts-shape with `sendFn` / `getMetadata` / `getLastMessage`. Backward-
  compat with the original `(handler, peerId, pod)` signature retained.
  Write throws clear "no send function wired" error when no sendFn.
- `web/clawser-workspace-init-mesh.js` — wires
  `discoveryManager.onPeerDiscovered`/`onPeerLost` to register/unregister
  peer device files.
- `web/clawser-snapshot-cli.js` — `save`/`restore`/`list`/`delete` use tar
  methods (writing to `~/.local/share/clawser/snapshots/{id}.tar`); fall
  back to IDB when no shell fs is available. List merges both backends.
- `web/test/clawser-fs-e2e.test.mjs` — added 2 tests: opts-only signature
  for mesh peer device, and explicit "no sendFn" error path.

---

## Files reviewed and left as-is

These were checked against current code and found to be either accurate or
appropriately scoped (e.g., historical audit records, ADRs).

- `README.md` — no stale claims found in the audit grep.
- `ARCHITECTURE.md` — high-level architecture description matches code.
- `CLAUDE.md` — repo-wide conventions doc; matches reality.
- `CONTRIBUTING.md` — process doc; not implementation-claim heavy.
- `SECURITY.md` — policy doc.
- `docs/API.md` — API reference; sampled, no stale claims.
- `docs/ARCHITECTURE-AUDIT.md` — March 2026 historical audit; intentionally
  preserved.
- `docs/ROADMAP-ARCHIVE.md` — historical archive; preserved.
- `docs/INTERNALS.md` — internal architecture; sampled, accurate.
- `docs/MODULES.md` — module manifest; matches current modules.
- `docs/FEATURES.md` — feature guide; matches current features.
- `docs/CONFIG.md` — config reference; not implementation-claim heavy.
- `docs/DEPLOYMENT.md`, `docs/SERVER.md` — operational docs.
- `docs/EVENT-LOG.md` — format spec.
- `docs/TESTING.md`, `docs/TOOLS.md`, `docs/PLUGINS.md` — reference docs.
- `docs/FIREFOX-COMPAT.md`, `docs/BROWSERMCP-EVALUATION.md` — eval docs.
- `docs/REMOTE-RUNTIME-CONTRACTS.md` — Phase 7A spec, accurate.
- `docs/WSH-INTO-CLAWSER.md` — covers Phase 7A modes; accurate.
- `docs/adr/ADR-REMOTE-RUNTIME-*.md` (5 files) — ADRs are immutable
  historical decisions; preserved.
- `docs/browsermesh/README.md` and per-area READMEs — high-level overviews;
  accurate.
- `docs/browsermesh/specs/*` (~80 files) — sampled the 12 with explicit
  "Implementation Status" sections; all accurate. Other specs are design
  documents without implementation claims.
- `docs/browsermesh/examples/*` (8 example apps) — illustrative; not
  implementation-claim docs.
- `docs/tutorials/*` (10 tutorials + README) — user-facing how-to; matched
  the features they cover at sample.
- `guide/*` (21 files) — auto-generated from `docs/data/*.yaml`; the data
  layer is now updated for tab completion. Other guide files remain
  accurate. (See follow-up below for unaddressed data-layer additions.)
- `docs/qa-report.md`, `docs/gap-closure-plan.md`, `docs/implementation-status.md` — actively maintained in this audit cycle.
- `.github/ISSUE_TEMPLATE/*`, `.github/PULL_REQUEST_TEMPLATE.md` — process
  templates.
- `web/packages/*/README.md` — package READMEs; preserved.

---

## Contradictions surfaced

None unresolved. The two contradictions found during the deep-dive
verification:

1. **Implementation-status A3/A6 said "Done", code said "library shipped
   without caller."** — Resolved by wiring the missing callers and
   demoting A3 to Partial.
2. **`docs/cli-enhancement-plan.md` was tagged "Draft"** while 4 of the 5
   features had shipped. — Resolved by adding the per-feature status block
   at the top.

The previously documented contradictions in `docs/implementation-status.md`
(SW persistent daemon, PBFT consensus, etc.) remain as-is — those describe
real partial implementations and are accurately classified.

---

## Recommended for archival or deletion

Brutally honest read of what should leave the live `docs/` tree:

- **`docs/unix-filesystem-architecture.md §22-34` (wnix v0.1-v0.4)** —
  recommend moving these ~600 lines to `.reference/wnix-design-notes.md`.
  They describe a different product (browser-as-OS) and dilute the focus
  of an otherwise actionable architecture doc. The aspirational banner
  added in this audit is a band-aid; relocation is the cleaner fix. Not
  done in this pass because relocation is a meaningful editorial act and
  the user explicitly said "Don't rewrite design docs that are
  intentionally aspirational. Just add a Status banner."
- **`docs/ARCHITECTURE-AUDIT.md`** — March 2026 historical audit. Could
  move to `docs/audits/` if more audits accumulate. Currently fine where
  it is; the date in the title makes its scope clear.
- **`docs/ROADMAP-ARCHIVE.md`** — explicit historical archive. Keep.
- **`docs/qa-report.md`** — prior QA report from this session's chain.
  Could rotate older audits into a `docs/audits/` subfolder to keep the
  top of `docs/` clean. Not done in this pass; the user can decide.
- **`docs/REVERSE-BROWSER-TERMINAL-BACKLOG.md`** — backlog now mostly
  shipped. Recommend either closing it (move to `ROADMAP-ARCHIVE`) or
  paring down to the remaining `wsh copy-id` stub item. Banner added
  for now.

Nothing recommended for outright deletion. Even speculative docs have
historical value as design conversations.

---

## Follow-ups not in scope (called out for the user)

- The auto-generated `guide/*.md` files might still be drifting from the
  `docs/data/*.yaml` source. The doc generator should be re-run after the
  yaml changes (added a Tab Completion entry to `shell.yaml`). I did not
  run the generator in this audit because I'm not certain which command
  invokes it; flagging for the user.
- `docs/data/*.yaml` files for other new features (tar snapshots, peer
  device files, RPC HTTP transport, PWA install) are not yet added. They
  would be additive entries; the audit didn't push on completeness because
  the existing entries weren't wrong, just incomplete.
- `docs/MODULES.md` doesn't list `clawser-tar.mjs`, `clawser-pwa-install.js`,
  `clawser-fs-env.mjs`, or several other new modules. Same incompleteness
  rather than wrongness call.
- iOS Safari compatibility audit (RM Phase 12) and various Batch-3 items
  aren't reflected in tutorials. Not user-facing yet, so probably fine.

---

## Test status

`node web/test/run-tests.mjs` → **8,846 tests passing, 0 failing.**

Counter drift across runs is test-discovery race (parallel runner); all
runs report 0 failures.

{
  "name": "clawser-docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "Documentation site for Clawser — built and copied into web/docs/ at build time",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && rm -rf ../web/docs && cp -r dist ../web/docs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@erisera/circuit": "github:erisera-code/circuit",
    "@astrojs/starlight": "^0.41.3",
    "astro": "^7.0.6"
  }
}
